/**
 * Functions for updating and displaying task list
 */

(function(window) {
    'use strict';

    let App = window.App || {};

    let UI = function() {
        
    };

    App.UI = UI;
    window.App = App;
})(window);